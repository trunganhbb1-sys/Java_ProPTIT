# Java là gì

* Java là một ngôn ngữ lập trình bậc cao, hướng đối tượng (OOP). Được sử dụng phổ biến trong phát triển phần mềm, trang web, game hay ứng dụng.
* Các đặc điểm:
  * Hướng đối tượng
  * Có tính đa nền tảng
  * Cú pháp khá giống C++
  * Có cơ chế tự động quản lý bộ nhớ (Garbage Collection)

# Lí do ra đời của Java

* **Lập trình thiết bị điện tử gia dụng:** Ban đầu Java được tạo ra (dự án Green Project - 1991) để viết phần mềm điều khiển các thiết bị thông minh như tivi, đầu đĩa, lò vi sóng…
* **Giải quyết điểm yếu của C++:** Các ngôn ngữ lúc bấy giờ quá phức tạp, lập trình viên phải quản lý bộ nhớ thủ công dẫn đến dễ rò rỉ bộ nhớ (memory leak) và phụ thuộc chặt vào phần cứng.
* **Tính độc lập phần cứng ("Viết 1 lần, chạy mọi nơi" - WORA):** Các thiết bị gia dụng có kiến trúc chip xử lý khác nhau. Nhóm phát triển cần một ngôn ngữ độc lập nền tảng, nghĩa là mã nguồn chỉ cần biên dịch một lần là có thể chạy được trên mọi thiết bị có máy ảo tương thích.

# Cách Java hoạt động, điều gì xảy ra khi chạy code Java

Mã nguồn Java hoạt động dựa trên cơ chế kết hợp giữa **biên dịch (compile)** và **thông dịch (interpret)** thông qua JVM.

## 1. Viết mã nguồn
* Bạn viết code Java và lưu vào tệp có phần mở rộng `.java`.
* Tệp này chứa mã nguồn dạng văn bản thuần túy, con người đọc hiểu được nhưng máy tính không thể thực thi trực tiếp.

## 2. Quá trình biên dịch (Compile)
* Khi dùng lệnh `javac` (nằm trong JDK), trình biên dịch sẽ đọc tệp `.java` và kiểm tra lỗi cú pháp.
* Nếu code hợp lệ, nó dịch mã nguồn thành **Bytecode** và lưu vào tệp có phần mở rộng `.class`.
* Bytecode không phải là mã máy của riêng hệ điều hành nào, mà là tệp mã trung gian dành riêng cho JVM hiểu.

## 3. Quá trình thực thi (Run)
* Máy ảo Java (**JVM**) trên từng hệ điều hành (Windows, macOS, Linux) sẽ đọc tệp `.class` này.
* JVM vừa thông dịch từng dòng vừa dùng trình biên dịch **JIT (Just-In-Time)** để chuyển Bytecode thành mã máy gốc cho CPU thực thi.

# Cấu trúc một chương trình Java

## 1. Khai báo package (nếu có)
```java
package com.example.demo;
```

## 2. Khai báo import thư viện (nếu cần)
```java
import java.util.Scanner;
```

## 3. Khai báo lớp chính (Class)
```java
public class Main {
    // Điểm khởi đầu của chương trình
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}
```

* `String[] args`: Mảng chứa các tham số dòng lệnh truyền vào khi chạy chương trình qua Terminal/CMD.

# Package là gì

* Trong lập trình Java, **package (gói)** là cơ chế dùng để gom nhóm các lớp (classes), giao diện (interfaces) có liên quan lại với nhau thành một thư mục logic.
* **Tác dụng chính:**
  * Tránh trùng lặp tên giữa các lớp.
  * Phân chia cấu trúc dự án rõ ràng theo từng module/tầng xử lý.
  * Hỗ trợ kiểm soát quyền truy cập thông qua Access Modifier.

# Syntax cơ bản

## 1. Phân biệt chữ hoa và chữ thường 
Java phân biệt hoa - thường rất khắt khe:
* Biến `diem` và `Diem` là hai biến hoàn toàn khác nhau.
* Phương thức `main` khác với `Main`.

## 2. Tên định danh 
Dùng để đặt tên cho lớp, biến, phương thức:
* Bắt đầu bằng một chữ cái (`a-z`, `A-Z`), dấu gạch dưới (`_`), hoặc ký hiệu (`$`).
* **Không** được bắt đầu bằng chữ số.
* **Không** được trùng với các từ khóa (Keywords) của Java.
* **Quy ước đặt tên (Naming Conventions):**
  * Tên lớp: Viết hoa chữ cái đầu mỗi từ theo kiểu PascalCase (`SinhVien`, `MainController`).
  * Tên biến/phương thức: Viết thường chữ cái đầu, viết hoa chữ cái đầu các từ tiếp theo theo kiểu camelCase (`diemTrungBinh`, `tinhTong`).

## 3. Từ khóa 
Là những từ Java đã định nghĩa sẵn mang ngữ nghĩa hệ thống:
* Ví dụ: `public`, `class`, `static`, `void`, `int`, `double`, `if`, `else`, `for`, `while`, `new`, `this`...

## 4. Dấu chấm phẩy ;
Mọi câu lệnh đơn trong Java bắt buộc phải kết thúc bằng dấu chấm phẩy (`;`). Nếu thiếu, trình biên dịch sẽ báo lỗi cú pháp ngay lập tức:
```java
int diemToan = 9;
```

## 5. Khối lệnh 
Các dòng lệnh liên quan được gom lại bên trong một cặp ngoặc nhọn `{}`:
* Dùng để xác định phạm vi hoạt động của lớp, phương thức, cấu trúc rẽ nhánh hoặc vòng lặp.

## 6. Chú thích trong mã nguồn 
Giúp giải thích ý nghĩa của code, trình biên dịch sẽ bỏ qua hoàn toàn:
* Chú thích một dòng: `// Nội dung chú thích`
* Chú thích nhiều dòng: `/* Nội dung chú thích */`

# Khai báo biến nguyên thủy

Cú pháp khai báo biến:
* Gán giá trị ngay: `kiểu_dữ_liệu tên_biến = giá_trị;`
* Khai báo trước, gán sau: `kiểu_dữ_liệu tên_biến;`

Java cung cấp 8 kiểu dữ liệu nguyên thủy cơ bản:

| Kiểu | Kích thước | Mô tả | Ví dụ |
| :--- | :--- | :--- | :--- |
| `byte` | 1 byte | Số nguyên rất nhỏ (-128 đến 127) | `byte tuoi = 20;` |
| `short` | 2 bytes | Số nguyên nhỏ | `short nam = 2026;` |
| `int` | 4 bytes | Số nguyên thông dụng | `int luong = 15000000;` |
| `long` | 8 bytes | Số nguyên rất lớn (thêm hậu tố `L`) | `long danSo = 8000000000L;` |
| `float` | 4 bytes | Số thực (thêm hậu tố `f`) | `float diem = 8.5f;` |
| `double` | 8 bytes | Số thực độ chính xác cao (mặc định) | `double pi = 3.14159265;` |
| `boolean` | 1 bit | Đúng hoặc sai (`true` / `false`) | `boolean daTotNghiep = true;` |
| `char` | 2 bytes | Ký tự đơn (dùng nháy đơn `''`) | `char xepLoai = 'A';` |


# Câu lệnh rẽ nhánh

## 1. Cấu trúc if, else if, else
Dùng để kiểm tra các điều kiện logic so sánh lớn/nhỏ hoặc khoảng giá trị.

```java
if (điều_kiện_1) {
    // Chạy khi điều_kiện_1 đúng
} else if (điều_kiện_2) {
    // Chạy khi điều_kiện_1 sai và điều_kiện_2 đúng
} else {
    // Chạy khi tất cả các điều kiện trên đều sai
}
```

## 2. Cấu trúc switch-case
Dùng khi so sánh một biến với các giá trị rời rạc cụ thể (số nguyên, chuỗi, ký tự, enum).

```java
switch (biến) {
    case giá_trị_1:
        // Khối lệnh 1
        break; // Thoát khỏi switch, tránh chạy tràn xuống case dưới
    case giá_trị_2:
        // Khối lệnh 2
        break;
    default:
        // Chạy khi không khớp với bất kỳ case nào ở trên
}
```

# Làm quen với vòng lặp

## 1. Vòng lặp for
Dùng khi bạn xác định trước số lần lặp cụ thể.

```java
for (khởi_tạo; điều_kiện; bước_nhảy) {
    // Khối lệnh thực thi
}
```

Ví dụ in các số từ 1 đến 5:
```java
for (int i = 1; i <= 5; i++) {
    System.out.println("Giá trị hiện tại: " + i);
}
```

## 2. Vòng lặp while 
Dùng khi chưa biết trước số lần lặp; vòng lặp duy trì chừng nào điều kiện vẫn còn đúng (`true`).

```java
while (điều_kiện) {
    // Khối lệnh thực thi
}
```

Ví dụ:
```java
int soDem = 1;
while (soDem <= 3) {
    System.out.println("Số đếm là: " + soDem);
    soDem++; // Tăng biến đếm để tránh lặp vô tận
}
```

## 3. Vòng lặp do-while 
Khối lệnh bên trong `do` luôn được chạy ít nhất một lần rồi mới kiểm tra điều kiện.

```java
do {
    // Khối lệnh chạy ít nhất 1 lần
} while (điều_kiện);
```

Ví dụ:
```java
int so = 5;
do {
    System.out.println("Chạy ít nhất 1 lần, số = " + so);
    so++;
} while (so < 5); // Điều kiện sai nhưng vẫn in ra 1 lần
```


# Mảng trong Java

Mảng (Array) là cấu trúc dữ liệu dùng để lưu trữ tập hợp cố định các phần tử có **cùng kiểu dữ liệu** trong một vùng nhớ liên tiếp.

## Đặc điểm quan trọng của Mảng
* **Cố định kích thước:** Khi đã khởi tạo kích thước cho mảng thì không thể co giãn được trong suốt vòng đời chương trình.
* **Chỉ số (Index):** Đánh số bắt đầu từ `0` đến `n - 1` (với `n` là độ dài mảng).

## 1. Khai báo và khởi tạo mảng

### Cách 1: Khai báo kích thước trước, gán giá trị sau
```java
// Mảng số nguyên có sức chứa 5 phần tử
int[] mangSoNguyen = new int[5];

mangSoNguyen[0] = 10;
mangSoNguyen[1] = 20;
```

### Cách 2: Khởi tạo mảng kèm giá trị trực tiếp
```java
int[] arr = {8, 9, 7, 10, 6};
```

## 2. Truy xuất và duyệt mảng
Dùng thuộc tính `.length` để lấy số lượng phần tử của mảng.

```java
public class ViDuMang {
    public static void main(String[] args) {
        int[] arr = {8, 9, 7, 10, 6};

        // 1. Duyệt mảng bằng for truyền thống
        for (int i = 0; i < arr.length; i++) {
            System.out.println("Phần tử tại index " + i + ": " + arr[i]);
        }

        // 2. Duyệt mảng bằng for-each (ngắn gọn khi chỉ cần đọc giá trị)
        for (int n : arr) {
            System.out.println("Giá trị: " + n);
        }
    }
}
```

# Class và Object

## 1. Class (Lớp) là gì?
* **Định nghĩa:** Class là một bản thiết kế hoặc một khuôn mẫu quy định các đặc điểm (thuộc tính) và hành vi (phương thức) mà các đối tượng tạo ra từ nó sẽ có.
* **Thành phần chính:**
  * **Thuộc tính (Fields):** Đại diện cho trạng thái, đặc điểm (ví dụ: họ tên, tuổi).
  * **Phương thức (Methods):** Đại diện cho hành động, chức năng (ví dụ: hiển thị thông tin, tính toán).

## 2. Object (Đối tượng) là gì?
* **Định nghĩa:** Object là một thực thể cụ thể (instance) được tạo ra từ Class.
* Nếu Class là bản vẽ của một chiếc xe máy, thì Object là từng chiếc xe thực tế chạy ngoài đường mang biển số và màu sắc riêng.

## 3. Phân biệt nhanh Class và Object
* **Class:** Tồn tại ở mức mã nguồn trừu tượng, chưa cấp phát vùng nhớ dữ liệu thực tế.
* **Object:** Thực thể sinh ra trong bộ nhớ Heap khi dùng từ khóa `new`.

# Từ khóa this, constructor, access modifier, getter, setter, từ khóa static

## 1. Từ khóa `this`
Là biến tham chiếu trỏ đến chính đối tượng hiện tại. Thường dùng để phân biệt thuộc tính của lớp với tham số truyền vào khi chúng trùng tên nhau.

## 2. Constructor (Hàm khởi tạo)
Là phương thức đặc biệt được gọi tự động khi dùng từ khóa `new` để tạo đối tượng.
* **Đặc điểm:** Tên constructor phải trùng hoàn toàn với tên lớp, không có kiểu dữ liệu trả về (kể cả `void`).

## 3. Access Modifier (Phạm vi truy cập)
Quy định quyền truy xuất tài nguyên của lớp:
* `private`: Chỉ truy cập được bên trong chính lớp đó.
* `protected`: Truy cập trong cùng package và các lớp con kế thừa.
* `public`: Truy cập tự do từ bất kỳ đâu.

## 4. Getter và Setter
Phương thức dùng để truy cập gián tiếp vào các thuộc tính `private`:
* **Getter:** Hàm đọc giá trị.
* **Setter:** Hàm gán giá trị, cho phép kiểm tra tính hợp lệ của dữ liệu trước khi lưu.

## 5. Từ khóa `static`
Thuộc về cấp độ **Class** chứ không phụ thuộc vào từng đối tượng riêng lẻ:
* Tất cả đối tượng đều dùng chung một vùng nhớ của biến `static`.
* Có thể gọi phương thức hoặc biến `static` trực tiếp qua tên lớp (`TenClass.tenHam()`) mà không cần `new`.

## 6. Code ví dụ tổng hợp

```java
public class SinhVien {
    // 1. Thuộc tính private (bảo mật dữ liệu)
    private String ten;
    private double diem;

    // Biến static dùng chung cho toàn bộ lớp SinhVien
    public static String truong = "PTIT";

    // 2. Constructor: Dùng 'this' để phân biệt thuộc tính và tham số
    public SinhVien(String ten, double diem) {
        this.ten = ten;
        this.diem = diem;
    }

    // 3. Getter và Setter
    public String getTen() {
        return this.ten;
    }

    public void setTen(String ten) {
        this.ten = ten;
    }

    public double getDiem() {
        return this.diem;
    }

    public void setDiem(double diem) {
        if (diem >= 0 && diem <= 10) {
            this.diem = diem;
        } 
    }

    // 4. Phương thức static
    public static void thongBao() {
        System.out.println("Danh sach sinh vien thuoc truong : " + truong);
    }
}
```
